---
description: Summarize content under a strict word limit with a fixed output structure
argument-hint: [content to summarize]
---

Summarize the following in 120 words max: "$ARGUMENTS". Output exactly: 5 bullets, then 1 risk, then 1 next step — no more, no less.

Adjust the word limit or output structure only if the user specifies different constraints in "$ARGUMENTS" — otherwise use the defaults above.
