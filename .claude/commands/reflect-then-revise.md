---
description: Draft, self-score, fix what scores low, return only the final version
argument-hint: [task]
---

Draft the answer for "$ARGUMENTS". Score it 1 to 5 on clarity, accuracy, usefulness. Fix any score under 4. Return only the final version and the three fixes applied.

Fill in the bracketed context above from "$ARGUMENTS" — ask for anything not provided rather than guessing. Ask for tables, citations, and stated assumptions where the prompt calls for them; don't invent a source or figure that isn't verifiable.
