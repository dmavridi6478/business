---
description: List assumptions and missing info before answering, flag critical ones
argument-hint: [task]
---

Before answering "$ARGUMENTS", list assumptions and missing info in 3 bullets. If any assumption is critical to the answer, state the safest default you will use. Then deliver the answer.

Fill in the bracketed context above from "$ARGUMENTS" — ask for anything not provided rather than guessing. Ask for tables, citations, and stated assumptions where the prompt calls for them; don't invent a source or figure that isn't verifiable.
