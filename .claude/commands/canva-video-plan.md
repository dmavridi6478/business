Recommend which AI Canva video format to use for "$ARGUMENTS" (the content
need — describe the platform, goal, and whether footage/a presenter
already exists), using `docs/procedures/canva-ai-video.md`.

1. If the platform, goal, or footage/presenter situation isn't stated,
   ask before recommending — the right format depends on all three, and
   guessing produces a plausible-sounding wrong answer.
2. Pick exactly one primary format from the procedure's 5 (animated
   design, talking presentation, video editing, AI voiceover,
   text-to-video) using its decision shortcut: existing footage → video
   editing; need a presenter, no camera crew → talking presentation; scale
   without recording → animated design (branded) or text-to-video
   (abstract/backgrounds); narration over existing visuals → AI voiceover.
3. Give the concrete steps for that format from the procedure doc, plus
   the correct export spec for the stated platform.
4. If the request would be better served by the heavier stack in the
   `video` skill (a custom AI avatar, programmatic/templated batch
   rendering, or a dedicated AI-generation model needing more control than
   a text prompt) rather than Canva's built-in suite, say so instead of
   forcing a Canva-only answer.
