---
description: Generate three internal drafts, return the single best plus why
argument-hint: [task]
---

Generate three internal drafts for "$ARGUMENTS". Return the single best final answer plus three bullet checkpoints that justify why it's the best of the three.

Fill in the bracketed context above from "$ARGUMENTS" — ask for anything not provided rather than guessing. Ask for tables, citations, and stated assumptions where the prompt calls for them; don't invent a source or figure that isn't verifiable.
