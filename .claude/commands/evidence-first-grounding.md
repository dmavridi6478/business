---
description: Answer using only the attached source, flagging anything unverifiable
argument-hint: [question and source/document]
---

Answer "$ARGUMENTS" using only the attached document or source provided. If a point is not present in it, say 'I cannot verify this.' Quote exact lines and list the section headers referenced.

Fill in the bracketed context above from "$ARGUMENTS" — ask for anything not provided rather than guessing. Ask for tables, citations, and stated assumptions where the prompt calls for them; don't invent a source or figure that isn't verifiable.
